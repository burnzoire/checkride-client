name: Test Suite

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v7

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: 20.x
        cache: 'npm'
        cache-dependency-path: app/package-lock.json

    - name: Install dependencies
      working-directory: ./app
      run: npm ci

    - name: Run tests
      working-directory: ./app
      run: npm run test:js -- --ci --coverage --maxWorkers=2

    - name: Extract coverage percentage
      working-directory: ./app
      run: |
        $coverage = Get-Content coverage/coverage-summary.json | ConvertFrom-Json
        $percentage = [math]::Round($coverage.total.statements.pct, 2)
        echo "COVERAGE=$percentage" >> $env:GITHUB_ENV
      shell: pwsh

    - name: Archive test results
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: test-results-node-20
        path: |
          app/coverage/
          app/__tests__/
        retention-days: 30

  lua-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7

    - name: Install Lua and LuaRocks
      run: |
        sudo apt-get update -qq
        sudo apt-get install -y lua5.1 liblua5.1-dev luarocks

    - name: Install Busted and LuaFileSystem
      run: |
        sudo luarocks install busted
        sudo luarocks install luafilesystem

    - name: Run Lua tests
      run: busted lua/
